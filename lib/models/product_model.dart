class ProductModel {
  final String name, image, description;
  final double price;

  const ProductModel({
    required this.name,
    required this.image,
    required this.description,
    required this.price,
  });
}
